name: test

on:
  push:
    branches: [master]
  pull_request:
    branches:
      - master

jobs:
  pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: test
        run: npm run test:ci
