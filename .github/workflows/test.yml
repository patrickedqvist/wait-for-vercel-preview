name: test

on:
  pull_request:
    branches:
      - main
      - preprod

jobs:
  pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: test
        run: npm run test:ci
